// Create this item as a Text parameter when possible.
// Replace only the path below; do not change the source workbook manually.
let
    DataFilePath = "C:\CHANGE_ME\sanitoral-project-performance-dashboard\data\raw\sanitoral_project_data.xlsx"
in
    DataFilePath